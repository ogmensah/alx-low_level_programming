#include <unistd.h>
#include "main.h"
/* more headers goes there */

/**
 * mul - it checks for upper case letters
 *@a: first parameter
 *@b: second parameter
 * Return: returns the product of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}

